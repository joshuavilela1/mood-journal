/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
};

export default nextConfig;

//! Have to add mjs / cjs for bun configruation depending on ESM or CommonJS
